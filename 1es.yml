parameters:
  - name: vmImage
    type: string
    default: 'ubuntu-latest'

  - name: container
    type: string

stages:
  - stage: BuildStage
    jobs:
      - job:
        pool:
          vmImage: ${{ parameters.vmImage }}
        container:
          image: ${{ parameters.container }}
        steps: ${{ job.steps }}