parameters:
- name: stages
  type: stageList
  default: []

- name: pool
  type: object
  default: {}

- name: containers
  type: object
  default: {}

pool: 
  vmImage: ${{ parameters.pool.vmImage }}

container:
  name: default_linux_container

stages: ${{ parameters.stages }}