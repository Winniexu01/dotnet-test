# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

pr: none
trigger: none

pool:
  vmImage: ubuntu-latest

variables:
  buildConfiguration: 'Release'

steps:
# - script: |
#     sleep 60
#     echo "Done waiting."
#     exit 0
#   displayName: Wait for 1 minutes
# - script: |
#     if [ "$(System.JobAttemptCount)" -gt 1 ]; then
#       echo "This is not the first attempt, skipping the rest of the steps."
#       exit 0
#     else
#       exit 1
#     fi
#   displayName: Error

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: $(Build.SourcesDirectory)/MergedManifest.xml
    artifactName: AssetManifests
  displayName: List all linux*-x64.tar.gz files