# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
  branches:
    include:
    - tmp

pr: none

# extends:
#   template: 1es.yml@self
#   parameters:
#     containers:
#       azurelinuxSourceBuildTestContainer:
#         image: wenwen01/preps:azurelinux-source
#     stages:
#       - stage: Stage
#         jobs:
#           - job: Setup
#             container: azurelinuxSourceBuildTestContainer
#             displayName: Setup Job
#             steps:
#               - script: |
#                   echo "scancode --version"
#                   scancode --version
#                 displayName: 'Setup'

          # - job: LicenseScan
          #   displayName: LicenseScan Job
          #   dependsOn: Setup
          #   steps:
          #     - script: echo "ddd"
          #       displayName: 'test'
pool:
  vmImage: Ubuntu-latest

stages:
  - stage: Stage
    jobs:
    - job: Steup
      container: mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-net9.0-opt-amd64
      displayName: Setup Job
      steps:
      - script: |
          echo "test"
        displayName: 'Steup'